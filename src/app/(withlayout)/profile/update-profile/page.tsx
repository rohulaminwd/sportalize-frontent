import UpdateProfile from '@/components/profile/UpdateProfile';
import React from 'react';

const page = () => {
    return (
        <div>
            <UpdateProfile />
        </div>
    );
};

export default page;